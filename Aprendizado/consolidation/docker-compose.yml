services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres-ensino
    restart: always
    environment:
      POSTGRES_USER: jp
      POSTGRES_PASSWORD: Novoprojeto1234567
      POSTGRES_DB: ensino_db
    ports:
      - "5555:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
